{
  "$defs": {
    "Channel": {
      "enum": [
        "email",
        "chat",
        "web"
      ],
      "title": "Channel",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "properties": {
    "channel": {
      "$ref": "#/$defs/Channel",
      "default": "web"
    },
    "customer_id": {
      "title": "Customer Id",
      "type": "string"
    },
    "id": {
      "title": "Id",
      "type": "string"
    },
    "message": {
      "maxLength": 20000,
      "minLength": 1,
      "title": "Message",
      "type": "string"
    },
    "received_at": {
      "format": "date-time",
      "title": "Received At",
      "type": "string"
    }
  },
  "required": [
    "customer_id",
    "message"
  ],
  "title": "Ticket",
  "type": "object"
}
